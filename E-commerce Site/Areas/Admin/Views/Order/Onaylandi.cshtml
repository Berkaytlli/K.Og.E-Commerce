@model IEnumerable<E_commerce_Site.Models.OrderHeader>
@{
    ViewData["Title"] = "Onaylandi";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<div class="btn-group d-grid gap-2 d-md-flex justify-content-md-end" role="group" aria-label="Basic outlined example">
    <a asp-action="Beklenen" asp-area="Admin" asp-controller="Orders" class="btn btn-outline-primary">Expected Orders</a>
    <a asp-action="Onaylanan" asp-area="Admin" asp-controller="Orders" class="btn btn-outline-primary active">Confirmed Orders</a>
    <a asp-action="Kargolanan" asp-area="Admin" asp-controller="Orders" class="btn btn-outline-primary">Shipped Orders</a>
    <a asp-action="Index" asp-area="Admin" asp-controller="Orders" class="btn btn-outline-primary">All Orders</a>
</div>
<br />
<div class="container">

    <table class="table table-striped table-bordered" id="myTable">
        <thead>
            <tr>
                <th>
                    Name
                </th>
                <th>
                    Surname
                </th>
                <th>
                    Phone
                </th>
                <th>
                    Order Status
                </th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @item.Name
                    </td>
                    <td>
                        @item.Surname
                    </td>
                    <td>
                        @item.PhoneNumber
                    </td>
                    <td>
                        @item.OrderStatus
                    </td>
                    <td>
                        @if (User.IsInRole(Diger.Role_Admin))
                        {
                            <a asp-action="Details" asp-route-id="@item.Id" class="btn btn-outline-info"><i class="bi bi-pencil"></i>Edit</a>
                        }
                        else
                        {
                            <a asp-action="Details" asp-route-id="@item.Id" class="btn btn-outline-info"><i class="bi bi-pencil"></i>Detail</a>
                        }


                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>
@section Scripts{
    <script type="text/javascript">
        $(document).ready(function () {
            $('#myTable').DataTable({
               
            });
        });
    </script>
}

